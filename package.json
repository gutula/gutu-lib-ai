{
  "name": "gutu-lib-ai",
  "private": true,
  "type": "module",
  "workspaces": [
    "framework/libraries/*"
  ],
  "scripts": {
    "build": "bun run --cwd framework/libraries/ai build",
    "typecheck": "bun run --cwd framework/libraries/ai typecheck",
    "lint": "bun run --cwd framework/libraries/ai lint",
    "test": "bun run --cwd framework/libraries/ai test",
    "test:unit": "bun run --cwd framework/libraries/ai test:unit",
    "docs:summary": "node scripts/docs-summary.mjs",
    "docs:check": "node scripts/docs-check.mjs"
  }
}
